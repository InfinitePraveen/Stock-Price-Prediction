import {esc,money,pct} from "./utils.js";export function shell(title,body){return `<section class="section-title"><span class="pill">ML STOCK FORECASTING</span><h1>${title}</h1></section>${body}`}export function mapping(c){return `<div class="mapping">${Object.entries({Date:c.date||"Not found",Open:c.open||"Optional",High:c.high||"Optional",Low:c.low||"Optional","Close / Price":c.close||"NOT FOUND",Volume:c.volume||"Optional"}).map(([k,v])=>`<div><b>${k}</b>${esc(v)}</div>`).join("")}</div>`}export function cards(m,p){return `<div class="grid"><div class="metric"><div class="label">Predicted Next Close</div><div class="value accent">₹ ${money(p.predictedClose)}</div></div><div class="metric"><div class="label">Latest Close</div><div class="value">₹ ${money(p.lastClose)}</div></div><div class="metric"><div class="label">Expected Direction</div><div class="value ${p.direction==="UP"?"up":"down"}">${p.direction}</div></div><div class="metric"><div class="label">Test RMSE</div><div class="value">₹ ${money(m.rmse)}</div></div></div>`}