export function line(canvas,values,title="Recent closing price"){const ctx=canvas.getContext("2d"),d=devicePixelRatio||1,w=canvas.clientWidth*d,h=canvas.clientHeight*d;canvas.width=w;canvas.height=h;ctx.clearRect(0,0,w,h);const min=Math.min(...values),max=Math.max(...values),pad=38*d,x=i=>pad+(w-pad*1.5)*i/Math.max(1,values.length-1),y=v=>h-pad-(h-pad*1.7)*(v-min)/Math.max(1e-9,max-min);ctx.strokeStyle=getComputedStyle(document.documentElement).getPropertyValue("--line");ctx.lineWidth=d;for(let i=0;i<5;i++){const yy=pad+(h-pad*1.7)*i/4;ctx.beginPath();ctx.moveTo(pad,yy);ctx.lineTo(w-pad,yy);ctx.stroke()}ctx.beginPath();values.forEach((v,i)=>i?ctx.lineTo(x(i),y(v)):ctx.moveTo(x(i),y(v)));ctx.strokeStyle="#08a878";ctx.lineWidth=2.5*d;ctx.stroke();ctx.fillStyle=getComputedStyle(document.documentElement).getPropertyValue("--muted");ctx.font=`${12*d}px system-ui`;ctx.fillText(title,pad,20*d)}