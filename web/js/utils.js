export const $=(s,r=document)=>r.querySelector(s);export function esc(v){return String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}export function norm(s){return String(s??"").trim().toLowerCase().replace(/[^a-z0-9]+/g,"")}export function num(v){if(v===null||v===undefined||v==="")return NaN;const n=Number(String(v).replace(/,/g,"").replace(/%$/,""));return Number.isFinite(n)?n:NaN}export function money(v){return Number.isFinite(v)?v.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2}):"—"}export function pct(v){return Number.isFinite(v)?`${(v*100).toFixed(2)}%`:"—"}export function mean(a){return a.length?a.reduce((x,y)=>x+y,0)/a.length:0}export function std(a){const m=mean(a);return Math.sqrt(mean(a.map(x=>(x-m)**2)))}