import {state} from "./state.js";import {shell,mapping,cards} from "./ui.js";import {esc,money,pct} from "./utils.js";
export function dashboard(){return shell("Stock price prediction",`<div class="hero"><div><h1>Upload a stock CSV. Train. Predict.</h1><p>Use historical OHLCV data to forecast the next trading day's closing price directly in the browser.</p><div class="actions"><a class="btn" href="#predict">Start prediction</a><a class="btn secondary" href="https://github.com/InfinitePraveen" target="_blank" rel="noopener">GitHub Profile</a></div></div><div class="panel"><h3>Interview-ready workflow</h3><div class="steps"><div class="step">Upload any compatible OHLCV CSV</div><div class="step">Detect the Close column as price</div><div class="step">Engineer time-series features</div><div class="step">Train Ridge Regression</div><div class="step">Forecast the next close</div></div></div></div><div class="section-title"><h2>About the Reliance dataset</h2></div><div class="grid2"><div class="panel"><h3>No Price column? No problem.</h3><p class="muted">Your screenshot contains <b>Close</b>. Close is a price series, so the app maps <b>Close → target price</b>. It can also recognize Last, Adj Close and Price.</p></div><div class="panel"><h3>Real browser prediction</h3><p class="muted">The previous version only previewed CSV data. This version actually trains a regression model and returns a next-day price estimate.</p></div></div>`)}
export function predict(){return shell("Predict next closing price",`<div class="panel"><label for="csv">Upload stock OHLCV CSV</label><input id="csv" type="file" accept=".csv,text/csv"><p id="status" class="muted">Recommended columns: Date, Open, High, Low, Close, Volume. Date + Close is sufficient.</p><div id="mapping"></div><div class="actions"><button id="train" class="btn" disabled>Train & Predict</button><button id="clear" class="btn secondary">Clear</button></div></div><div id="result" class="hidden"></div>`)}
export function data(){if(!state.rawRows.length)return shell("Dataset explorer",`<div class="panel"><h3>No dataset loaded</h3><p class="muted">Upload a CSV from <a href="#predict">Predict</a>.</p></div>`);return shell("Dataset explorer",`<div class="panel"><h3>${esc(state.fileName)}</h3><p>${state.rawRows.length.toLocaleString()} raw rows · ${state.cleanRows.length.toLocaleString()} valid price rows</p><p class="small muted">Detected Close / Price: <b>${esc(state.columns.close)}</b> · Date: <b>${esc(state.columns.date)}</b></p><div class="table-wrap"><table><thead><tr>${state.headers.map(c=>`<th>${esc(c)}</th>`).join("")}</tr></thead><tbody>${state.rawRows.slice(0,12).map(r=>`<tr>${state.headers.map(c=>`<td>${esc(r[c])}</td>`).join("")}</tr>`).join("")}</tbody></table></div></div>`)}
export function model(){return shell("Model",`<div class="grid2"><div class="panel"><h3>Ridge Regression</h3><p class="muted">A regularized linear regression model is implemented directly in JavaScript. Inputs are standardized before fitting.</p></div><div class="panel"><h3>Features</h3><p class="muted">Lagged closes, returns, SMA gaps, volatility, OHLC ranges and volume change.</p></div></div>`)}
export function evaluation(){if(!state.metrics)return shell("Evaluation",`<div class="panel"><h3>Train a model first</h3><p class="muted">Upload a CSV and click Train & Predict.</p></div>`);const m=state.metrics;return shell("Evaluation",`<div class="grid"><div class="metric"><div class="label">MAE</div><div class="value">${money(m.mae)}</div></div><div class="metric"><div class="label">RMSE</div><div class="value">${money(m.rmse)}</div></div><div class="metric"><div class="label">R²</div><div class="value">${m.r2.toFixed(3)}</div></div><div class="metric"><div class="label">Direction Accuracy</div><div class="value">${pct(m.direction)}</div></div></div><div class="panel" style="margin-top:18px"><h3>Holdout evaluation</h3><p class="muted">The final 20% of the time series is kept unseen during initial evaluation. The final forecasting model is then retrained using all usable historical samples.</p></div>`)}
export function about(){return shell("About",`<div class="panel"><h2>Praveen Kumar</h2><p class="muted">Data Science & Machine Learning portfolio project.</p><p>This web app turns a stock forecasting notebook into an interviewer-friendly interactive application.</p><a class="btn" href="https://github.com/InfinitePraveen" target="_blank" rel="noopener">github.com/InfinitePraveen</a></div><div class="panel" style="margin-top:18px"><h3>Disclaimer</h3><p class="muted">Educational forecasting demonstration only. Not financial advice.</p></div>`)}